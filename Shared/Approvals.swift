import Foundation

struct Approvals {
    
    private static let defaults = UserDefaults(suiteName: "group.io.balance.Approvals")
    
    func getApprovals(for: URL) -> [String]? {
        return nil
    }
    
}
